{
  "name": "my-theme",
  "version": "1.0.0",
  "description": "## Features",
  "main": "index.js",
  "scripts": {
    "build:css": "npx sass assets/scss/main.scss static/css/main.css --style=compressed",
    "build:js": "npx esbuild assets/js/main.js --bundle --minify --outfile=static/js/main.js",
    "build": "npm run build:css && npm run build:js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^5.3.3"
  }
}
